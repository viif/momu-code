[project]
name = "momu-code"
version = "0.1.0"
description = "参考 Learn Claude Code 实现的 Agent Harness 工程"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.100.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
